<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.EmpRoleMapper">

    <!-- 根据员工ID查询角色列表 -->
    <select id="selectRolesByEmpId" resultType="org.example.pojo.Role">
        SELECT r.id, r.name, r.code, r.description, r.create_time, r.update_time
        FROM role r
        INNER JOIN emp_role er ON r.id = er.role_id
        WHERE er.emp_id = #{empId}
    </select>

</mapper>
