<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.DictDataMapper">

    <select id="selectByType" resultType="org.example.pojo.DictData">
        SELECT id, type, label, value, sort, create_time
        FROM dict_data
        WHERE type = #{type}
        ORDER BY sort ASC, id ASC
    </select>

    <select id="selectAll" resultType="org.example.pojo.DictData">
        SELECT id, type, label, value, sort, create_time
        FROM dict_data
        ORDER BY type ASC, sort ASC, id ASC
    </select>

</mapper>
